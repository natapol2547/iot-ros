[develop]
script_dir=$base/lib/iot_robot_drivers
[install]
install_scripts=$base/lib/iot_robot_drivers
[tool:pytest]
# launch_testing's pytest plugin fails to load under the pytest in the pixi environment,
# and these tests do not need it
addopts = -p no:launch_testing -p no:launch_ros
